<div class="container mt-4">
  <div class="row">
    <div class="col-md-6">
      <img class="img-fluid" src="{{animal.imageUrl}}" alt="{{animal.name}}" />
    </div>
    <div class="col-md-6">
      <h1>{{animal.name}} | {{animal.age}}</h1>
      <p id="description">{{animal.description}}</p>
    </div>
  </div>
</div>